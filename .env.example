# TheBlueAlliance read API key (https://www.thebluealliance.com/account).
# Optional here — it can also be set later in the web UI under Settings.
TBA_API_KEY=

# Unraid share ownership (match the user/group that owns your video share).
PUID=99
PGID=100
